# Legacy Next.js backend

The frontend now uses Supabase Auth and the PostgreSQL RPC in ../supabase/. This Next.js app is not required to run TomTube. Start the new local backend from the repository root with `npm run backend:start`, then `npm run backend:configure`.

These files are retained as reference for migrating existing MongoDB/KV data. See the root README for setup and migration notes.
